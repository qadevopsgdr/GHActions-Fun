name: workflow - Matrix Test- operatesys
on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        operatesys: [ubuntu-latest, windows-latest]
    steps:
      - name: Print OS
        run: echo "Running on ${{ matrix.operatesys }}"
